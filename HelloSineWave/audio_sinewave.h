/* 
    functions for initializing and adjusting the oscillator. 
    more infor in audio_sinewave.c
*/

void synth_stop(void);
void synth_init(void);
void synth_freq(int freq);
void synth_amp(int freq);

